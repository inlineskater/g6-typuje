# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

**Rynek Proroctw G6** — a company prediction market web app with virtual coins, a shared Texas Hold'em table, and small office games. Polish UI. Single static file (`index.html`) backed by a hosted Supabase project and Supabase Edge Functions for server-owned game actions.

Live URL: `https://inlineskater.github.io/rynek-proroctw-g6/`

## Deployment

There is no build step. Push to `main` → GitHub Actions copies `index.html` to GitHub Pages. That's the entire pipeline.

To apply database changes: paste the relevant SQL into the Supabase SQL Editor (Dashboard → SQL Editor → Run). There is no migration runner.

- `supabase/schema.sql` — core prediction-market schema, run once on a fresh project
- `supabase/arcade.sql` — paid „Wszystkie Gry" archive scores; clients have SELECT-only table access, while the payment-linked score RPC validates the game/score cap and serializes submissions so one entry cannot be reused concurrently
- `supabase/store.sql` — reward shop tables/RPCs used by the Sklep tab
- `supabase/marketplace.sql` — peer-to-peer marketplace ("Targowisko") tables/RPCs; any user can list IRL goods/services; winning/buy coins transfer from buyer to seller (not burned)
- `supabase/poker.sql` — poker tables, RLS, realtime publication, and leaderboard stack accounting
- `supabase/functions/poker-action` — authenticated Edge Function that owns poker state transitions, hidden cards, and chip accounting
- `supabase/plinko.sql` / `supabase/functions/plinko-action` — „Plinko G6" solo casino game; the Edge Function owns each drop's randomness, path, payout, and coin accounting, while the frontend only animates the trusted server result
- `supabase/mines.sql` / `supabase/functions/mines-action` — „Miny G6" casino game; clients read own sanitized round/history only, hidden mine tiles live in `mines_round_secrets`, and all reveals/cashouts are owned by the Edge Function
- `supabase/crash.sql` / `supabase/functions/crash-action` — „Rakieta" (Crash) **solo** casino game; each player flies their OWN rocket (`crash_rounds`, one `running` row per user), the hidden crash point/time lives in `crash_round_secrets`, and the Edge Function (`start`/`state`/`cash_out`) owns the RNG, the lazy bust-on-read resolve, and the payout. Results land in `crash_spins` (wired into `economy-stats`/`hazard-views`/`coin-inflow-stats`). No shared table, no realtime; the client animates a freeze-proof per-frame delta-time clock and polls `state` to catch the crash. Postaw runs a **client-side 3 s launch countdown** (`CRASH_COUNTDOWN_MS`, big 3-2-1 digits + canvas ignition/smoke via `drawCrashScene('countdown', …, ignition)`) before `start` is invoked — coins commit only at zero, and a frozen RAF (tab switched mid-countdown) aborts the launch instead of firing it unwatched; `start` then creates the round with `started_at = now() + LAUNCH_HOLD_MS` (1 s in the future), so the client receives the response while the multiplier is still pinned at x1.00 (`elapsedMs` is NEGATIVE during the hold; `crashTick` renders the START! liftoff frame until it crosses zero) — liftoff is synchronized on screen and the rocket never has an unseen head start. **Cash-out fairness contract (WYSIWYG):** the WYPŁAĆ button value is a CONTRACT — `cash_out` pays the claimed `atMultiplier` EVEN ABOVE the hidden crash point (phantom cents the blind display painted before the reveal reached it), bounded by (a) arrival ≤ `CASHOUT_GRACE_MS` (450 ms) after `crash_at` and (b) a cap of `multAt(server elapsed + CLIENT_AHEAD_TOL_MS)` (250 ms legit display lead, mirrored by the `crashShownMs` clamp in `index.html`); a bot claiming the cap every round still loses ≈2% (`(1-HOUSE_EDGE)·exp(CRASH_GROWTH·700) ≈ 0.981`), so `cashout_multiplier` may legitimately exceed `crash_point` in `crash_spins`. Busts are revealed IMMEDIATELY (`resolveDueRound` flips the round the instant `now() ≥ crash_at`) and the grace works even on a round a racing 250 ms state poll just flipped to `busted` — `cash_out` locks the latest running-or-recently-busted round and converts the recorded 0-win spin back into the win (supersedes both the strictly-below-crash-point rule and the earlier delayed-reveal fix). To keep honest clicks inside the grace window, `start` schedules a **bust nudge** via `EdgeRuntime.waitUntil`: the worker sleeps until `crash_at`+120 ms, then POSTs a service-role Realtime broadcast (topic `crash_<roundId>`, event `bust`, empty payload — no secrets) that the client (`crashSubscribeBustNudge`) answers with a `state` pull, landing the explosion in ~150 ms; the 250 ms poll remains the fallback when the worker is evicted (>~150 s flights are capped by `MAX_MULT` anyway). **Parity contract:** `CRASH_GROWTH = Math.log(2)/15000` (doubles every 15 s — slowed from 10 s in 2026-07 so a given latency gap costs less multiplier) must match between `index.html` and the Edge Function
- `supabase/wheel.sql` / `supabase/functions/wheel-action` — displayed to players as **„Koło Żubra G6"** 🦬 (a Żubrówka/bison-grass-vodka reskin of the wheel; every internal identifier stays `wheel` — `wheel_spins`/`wheel_rounds`/`wheel_round_bets`, `wheel-action`, `WHEEL_*` constants, the `'wheel'` game key in stats views/`ACT_GAME_LABELS` — only user-facing strings changed) — a SHARED-ROUNDS casino game: ONE communal wheel with a SINGLE ordered set of 20 segments, no risk tiers (a bet is just a stake): 1× ×10 (jackpot 🦬), 3× ×2, 2× ×1 (ZWROT — stake back), 2× ×0.5 (half stake back), 12× ×0. Sum = 19.0 → **RTP 95.0%**. A round opens `betting` the moment the first player bets; `wheel_rounds` (`id`, `status`, `spin_at`, `segment_index`, `resolved_at`) and `wheel_round_bets` (`round_id`, `user_id`, `nick_snapshot`, `total_bet`, `ready`, `multiplier`, `total_won`, `UNIQUE(round_id,user_id)` — one bet per player per round) are both public-SELECT/no-client-write, realtime-published tables, mirroring the roulette pattern; `wheel_rounds_single_betting_idx` (partial unique index on `status='betting'`) guarantees at most one open round at a time. **Coin timing follows the ROULETTE convention:** `bet` only validates the caller's balance (locks the profile row FOR UPDATE, does not deduct) — coins move for every bet at once when the round resolves. **Resolution is lazy-on-read, crash-style:** `resolveDueRound` (SELECT ... WHERE status='betting' AND spin_at<=now() FOR UPDATE SKIP LOCKED) runs at the top of `state`, `bet`, and `ready`, so the frontend's 1 s poll on `state` (while a round is `betting`, gated by tab-active+visible) is what actually fires a due spin — realtime (`wheel_rounds`/`wheel_round_bets` postgres_changes) only tells clients something changed and triggers the same reload, it never drives the resolve itself. The betting window is `WHEEL_BETTING_WINDOW_MS` = 15 s (duplicated between `index.html` and the Edge Function; the server's `round.spinAt` is always authoritative — the client computes a clock offset from `serverNow` on every response for a synchronized countdown/spin across tabs). After betting, the same button becomes a `ready` vote: the Edge Function marks that bet's `ready` flag under the round lock, and when no unready bets remain it pulls `spin_at` forward to `LEAST(spin_at, now() + WHEEL_READY_GRACE_MS)` with a 3 s sync grace; a solo bettor can therefore fast-start too. **Lock ORDER is round row, then profile row**, identical in `bet()` (find-or-create the open round FOR UPDATE, then lock the caller's profile), `ready()` (round only), and `resolveOneRound()` (the due round is already locked via `resolveDueRound`'s FOR UPDATE before any bettor's profile row is touched) — so the paths cannot deadlock. The Edge Function draws one `segment_index` (0-19) server-side via `crypto.getRandomValues` rejection sampling and pays every bet on the round `floor(total_bet × SEGMENTS[segment_index] × luck)`, mirroring each payout into `wheel_spins` (unchanged shape minus the removed `risk` column — still one row per resolved bet, hazard/economy views need no changes) — a bettor who spent their coins elsewhere before the draw is skipped (`multiplier=0, total_won=0`, no charge, no payout). **Parity contract:** the ordered 20-element multiplier array (`WHEEL_SEGMENTS` in `index.html`, `SEGMENTS` in the Edge Function) must stay byte-for-byte identical — it literally IS the wheel. Gets the „Amulet Bezwstydnego Fartu" ×1.01 casino-luck boost like the other house games (`hasCasinoLuck`, copied verbatim from `plinko-action`), applied once per round draw (same luck roll for every bettor); `state` also returns `casinoLuckUntil` (aggregate `MAX(expires_at)` via `casinoLuckStatus`). Every house-casino tab shows a communal amulet banner with its game-specific effect and exact expiry (or an inactive hint) linked to the shop; the shop card shows the exact projected end date if bought now, stacking the item's duration onto the active shared window. House game: coins move directly on `profiles.coins`, no `coin_transactions` ledger rows. Frontend (`#tab-wheel`): a live "Zakłady tej rundy" panel shows every bet as it's placed (nick/stake/ready, gaining a win/loss result after resolve), a communal "Ostatnie rundy" strip (last 10 resolved, sector + biggest winner), plus the existing personal "Twoje zakręcenia" history from `wheel_spins`; after the bet is accepted, the POSTAW button (`.rl-btn.is-gold`) becomes the ready control. The canvas (`drawWheelBoard`) renders a single big ring (all the visual budget the removed risk tiers freed up — oversized multiplier labels, a decorative dark bezel, a gold bezel ring) painted in a **Żubrówka palette** (0x deep bottle green, 0.5x dried-straw/hay, 1x pale meadow green "ZWROT", 2x vivid bison-grass chartreuse, 10x molten gold with a 🦬 in the label) + a ~40-dot warm-gold LED rim that idle-pulses and CHASES during a spin + a dark-green center hub with a single tapered blade-of-grass bezier (the classic bottle motif) and a 🦬 mark, all driven by one continuous `requestAnimationFrame` loop (`wheelFrame`/`wheelUpdateAnimState`) that walks `idle → countdown → blind → landing → result`: a BLIND constant-velocity spin (`WHEEL_BLIND_SPEED`, ~2.5 rev/s) starts the instant `spin_at` passes locally with no result yet, then blends into an ease-out-cubic landing (`WHEEL_LANDING_DURATION_MS` = 3.5 s, whole-turn math reused from the old solo spin) once `segment_index` arrives; a tab that loads mid-spin with the result already known skips straight to a full reveal-style landing. `plinkoReducedMotion()` skips the blind spin/LED chase and jumps to a 300 ms settle. The nav button and page title carry a small pulsing „NOWOŚĆ" pill (`.wheel-nowosc-badge`, same pulse recipe as `.seasonal-dot`). Stats wired into `hazard-views.sql` (`hazard_stats.wheel_pl`, `game_transactions` UNION — hazardista column header shown as „Żubr"), `coin-inflow-stats.sql`, and `economy-stats.sql` (`hazard_house_net`) — all unchanged, since they only read `wheel_spins`. Run order: `wheel.sql`, then re-run `hazard-views.sql` + `coin-inflow-stats.sql` + `economy-stats.sql`.
- `supabase/whack-boss.sql` — Whack-a-Boss rounds, weekly/all-time leaderboard views, and scheduled weekly prize payout
- `supabase/functions/whack-boss-action` — authenticated Edge Function that issues and validates 18-second Whack-a-Boss rounds
- `supabase/bug-jumper.sql` / `supabase/flappy-pants.sql` / `supabase/snake.sql` / `supabase/invoice-horde.sql` / `supabase/var-patrol.sql` / `supabase/egg-catch.sql` / `supabase/super-mariusz.sql` / `supabase/popup-panic.sql` — seasonal arcade games (Bug Jumper, „3 Pary Spodni", Snake, „Najazd Ticketów" — internal gameType `invoice_horde`, VAR Patrol — internal gameType `var_patrol`, „Łap Jajka" — internal gameType `egg_catch`, „Super Mariusz" — internal gameType `super_mariusz`, „Zamknij Popupy!" — internal gameType `popup_panic`); each adds `*_rounds`/`*_scores`/`*_weekly_awards` tables, three leaderboard views, an `award_*_week()` payout, realtime publication, and a Monday `pg_cron` job after the week closes
- `supabase/functions/bug-jumper-action` / `supabase/functions/flappy-pants-action` / `supabase/functions/snake-action` / `supabase/functions/invoice-horde-action` / `supabase/functions/var-patrol-action` / `supabase/functions/egg-catch-action` / `supabase/functions/super-mariusz-action` / `supabase/functions/popup-panic-action` — authenticated Edge Functions that issue and validate seasonal-game rounds
- `supabase/season-award-gating.sql` — idempotent; season-gates the weekly award cron jobs via `seasonal_game_for_week()` and schedules them for **Monday 00:00 Europe/Warsaw** with a dual-UTC-hour + local-hour gate that remains correct across DST; must be kept in sync with the rotation constants in `index.html` and re-run when they change
- `supabase/polish-midnight-schedules.sql` — run last after garden watering, seasonal games/gating, and farm seasonal SQL. Production-safe consolidation migration: makes the Zen-garden daily watering allowance use the Warsaw calendar date, schedules all weekly seasonal/farm payouts for **Monday 00:00 Europe/Warsaw**, and aligns physical rotten-lot cleanup to Warsaw midnight; the dual UTC schedules are internally Warsaw-hour-gated for DST safety
- `supabase/fix-client-meta-double-encoding.sql` — idempotent; normalizes double-encoded `client_meta` on seasonal `*_scores` tables (one-time migration + BEFORE INSERT trigger), required for the leaderboard base/bonus score split
- `supabase/football.sql` — World Cup 2026 fixed-odds betting ("Mundial"); adds `football_matches`/`football_bets` tables (text event ids, SELECT-only for clients), extends the `leaderboard` view to count open bet stake, realtime publication, and an **every-6h** `pg_cron` job that `net.http_post`s the Edge Function to refresh odds and settle results. **Before running, replace the `__PROJECT_REF__`/`__FOOTBALL_CRON_SECRET__` placeholders in the cron block.**
- `supabase/functions/football-action` — Edge Function (`verify_jwt = false`) that owns all football writes: `state` (public read), `bet` (JWT-validated fixed-odds bet that locks server-side odds), and `cron` (`x-cron-secret`-gated sync+settle pulling events/odds/results from **The Odds API v4**)
- `supabase/economy-stats.sql` — idempotent; adds `economy_stats()` SECURITY DEFINER aggregate RPC that returns server-wide coin supply breakdown (cash, market positions, football/poker/auction escrow, hero items, accessories) plus per-player `holdings` array and `richest` for the **Skarbiec G6** panel on the Statystyki tab
- `supabase/canvas.sql` — „Wspólne Płótno" collaborative pixel canvas; adds `canvas_pixels` (one shared 192×108 board, public SELECT) + `canvas_cooldowns` (own-row SELECT, now unused) tables, realtime publication on `canvas_pixels`, and the original `place_pixel(x,y,color)` SECURITY DEFINER RPC (its per-user cooldown is superseded — see `canvas-free-for-all.sql`). Idempotent (`CREATE TABLE IF NOT EXISTS` / `CREATE OR REPLACE`); the bounds (192×108) live in the RPC and mirror `CANVAS_W`/`CANVAS_H` in `index.html`
- `supabase/canvas-free-for-all.sql` — run after `canvas.sql`; idempotent (`CREATE OR REPLACE`). Redefines `place_pixel` to drop the 2h-free/pay-1-coin split — every pixel is now free for every user, always; `canvas_cooldowns` is left in the schema but no longer read or written
- `supabase/canvas-paint-log.sql` — run after `canvas-free-for-all.sql` + `lottery.sql`; idempotent. Adds `canvas_paint_log` (no client grants — written/read only by SECURITY DEFINER functions) and redefines `place_pixel` to insert one log row per paint, fixing the Loteria's "Wspólne Płótno" ticket count: `canvas_pixels` alone is last-write-wins, so a player's earlier painting days disappear once someone else paints over that pixel. `mundial_lottery_standings()`'s `canvas` CTE is redefined to union days from `canvas_pixels` (current-owner, the old lower-bound proxy) with `canvas_paint_log` (accurate going forward) so no one's existing tally regresses. Re-run after re-running either superseded file.
- `supabase/lottery-fixes.sql` — run LAST among the lottery files (after `lottery.sql` + `canvas-paint-log.sql`, and re-run after re-running either — both carry superseded `mundial_lottery_standings()` copies with ⚠️ notes); idempotent. Fixes two ticket-count bugs found in the 2026-07-16 audit: **Targowisko overcount** — every cancel path (`cancel_marketplace_listing`, no-bid settle, the unpaid-tax void in `nft-merge-fixes.sql`) sets `settled_at` with `status='cancelled'`, so the `settled_at IS NOT NULL` filter credited sellers for cancelled listings; now filters `status='settled'`. **Farma undercount** (same lost-history class as the canvas bug) — `farm_inventory` lots are DELETEd when fully sold and purged when rotten, erasing "days harvested"; the immutable `coin_transactions` ledger (`reason='farm_crop_sale'`) is unioned in as durable evidence (tile/box buys deliberately excluded — buying isn't farming). All other categories were audited correct, including `bank_net` parity with `computeFootballHouseStats().realizedProfit`.
- `supabase/farm.sql` — „Farma" (Phase 1): a shared **13×4** owned-tile grid + collectible plant-card economy exposed as **🌱 Ogródek** (the old zen `water_plant` tab is hidden from nav but its plant/pot code is still reused for migrated plant tiles). Adds `farm_tiles` (sparse, public SELECT, PK `(x,y)`), `farm_card_defs` (seeded catalog), `farm_collection`/`farm_inventory` (own-row SELECT), `farm_market` (dynamic NPC prices, public SELECT), `farm_nft_instances` (serialized NFT cards, public SELECT), `farm_user_state` (own-row SELECT — owned sealed `boxes` + `tile_vouchers` + one-time starter-gift flags); realtime on tiles/market/collection/inventory/user_state; and SECURITY DEFINER RPCs `buy_farm_tile` (claim via `INSERT ... ON CONFLICT DO NOTHING`; consumes a free-tile voucher if held, else escalating BURN with price `min(50000, floor(350*2^owned_tiles))`), `claim_farm_starter` (idempotent one-time welcome gift: **3 free boxes** + a tile voucher **guaranteed within those first 3 openings only while the player has zero territory**), `buy_farm_lootbox` (BURN 100/box → sealed boxes), `open_farm_lootbox` (consumes one owned box, no coin cost; weighted `random()` draw of **3 distinct** cards → `cards[]`; each owned NFT divides future NFT weights by 3; natural voucher chance is `0.07 / 3^(owned tiles + held vouchers)` and returns `{voucher:true}` on drop), `level_up_card` (consume dupes + coins BURN), `plant_crop`/`harvest_crop` (server-timed growth, crops minted to inventory as **per-harvest lots that ROT 5 days after collection**; `farm_inventory` is `(id, user_id, crop_type, qty, harvested_at, expires_at)`; `plant_crop` rejects `acquired_via='migration'` plant tiles → `zen_tile`), `sell_crop_to_npc` (row-locked **fluctuating** price, coins MINTED, consumes lots FIFO by soonest `expires_at`), `farm_rot_cleanup` (daily pg_cron purge of expired lots). **Pricing is Animal-Crossing "stalk market" style:** `farm_market` carries `base_price` (catalog **MAX**/ceiling), `anchor_price` (the current "normal", rolled **twice daily at 00:00 & 12:00 Europe/Warsaw** to **30–100% of base**, Balanced avg ≈0.57×), and `cur_price` (live; a sale dips it **once per transaction scaled by size** — `min(40%, 0.5%×qty)`, batch charged the pre/post average — then it recovers toward `anchor_price` at ~12%/hr, floor 30% of base). The roll lives in `roll_farm_prices()`/`maybe_roll_farm_prices()` in `farm-price-history.sql`. Boxes are **bought first (Sklep), opened later (🎒 Mój Majątek)** — two-step. Idempotent; includes a run-once migration placing each existing gardens row (linked by `zen_garden_id`) on one **plant tile** (`acquired_via='migration'`, plant-only — delete+recreate, deterministic). **Bounds (13×4) mirror `FARM_W`/`FARM_H` in `index.html`; coin reasons `farm_tile_buy`/`farm_box_buy`/`card_levelup` (burn) and `farm_crop_sale` (mint) must stay in sync with `economy-stats.sql` + `leaderboard-net-worth-items.sql` (which also value owned boxes ×100 + vouchers ×350 in `farm_assets`). For prod-only balance updates use `supabase/farm-anti-hoarding.sql`, not full `farm.sql`, to avoid reseeding market prices.**
- Farm land tax (**soft cap**): the fair share is `ceil(normal_capacity / active_farm_participants)` normal (non-migration) tiles per player. Buying/planting past it is allowed; the excess is taxed `1000 * excess^2` daily with 10% daily debt interest, and **only unpaid debt** blocks buying more tiles (`farm_assert_can_expand`); planting is never blocked (`farm_assert_can_plant` is a no-op kept for call-site compatibility, and `blocked_planting` in the tax quote JSON is hardcoded `false`). Autopaid from crop/marketplace proceeds (`farm_apply_land_tax_autopay`) and manually via `pay_farm_land_tax`. The assessment runs at **00:00 Europe/Warsaw** for the previous completed day; first payment is **Friday 03.07.2026 00:00** for **Thursday 02.07.2026**. Unpaid `land_tax_debt` is a **net-worth liability**: subtracted once inside `user_assets_value` (which both the `leaderboard` view and `economy_stats()` holdings/richest call), reported as `farm_tax_debt` by `user_net_worth_breakdown` (subtracted from `total`; red row in the Bilans Net Worth UI) and by `economy_stats()` (informational total — NOT subtracted from `total_supply`).
- `supabase/farm-price-history.sql` — „Farma": fluctuating-price engine + per-crop NPC price history for the **📈 Ceny** hub tab. Adds `farm_price_history` (public SELECT), a trigger logging every `farm_market.cur_price` change (each **roll** and each **sale**), `roll_farm_prices()` (draws a regime per crop → sets `anchor_price`+`cur_price` to **30–100% of base**, Balanced bands) + `maybe_roll_farm_prices()` (Warsaw-hour gate so an hourly pg_cron only rolls at **00:00 & 12:00**), `snapshot_farm_prices()` (records the recovery toward `anchor_price`, every 2h), and a one-time backfill from past `farm_crop_sale` rows. Frontend: `buildFarmMarketBody`/`farmPriceChart` (inline SVG **7-day** line chart per crop, dashed line = MAX) + `loadFarmSalesFeed` (public 🧾 crop-sales feed). **The regime bands (30–100%, Balanced split) live only in `roll_farm_prices()` (server-owned, `random()`). The per-transaction sale math — drop `min(40%, 0.5%×qty)`, 12%/hr recovery toward `anchor_price`, 30%-of-base floor — is duplicated between `sell_crop_to_npc` (`farm.sql`) and the client preview `farmSellQuote` (`index.html`); keep the two in sync.**
- `supabase/farm-marketplace.sql` — „Farma" Phase 3 (run after `marketplace.sql`+`farm.sql`): lets players auction/sell plant cards, serialized NFTs, and empty non-migration farm territories to each other by extending the Targowisko engine with item linkage + ownership transfer + `farm_nft_transfers` provenance/price log; idempotent
- `supabase/nft-leveling-rework.sql` — moves NFT level from `farm_collection` onto each `farm_nft_instances` row; adds `level_up_nft(hero, fuel)` merge RPC (fuel is burned forever), instance-aware `plant_crop(p_x,p_y,p_species,p_instance_id)` + `harvest_crop` (clears `planted_instance_id`), NFT-skipping lootbox, and level-aware listings/views. Run after `farm.sql`+`farm-marketplace.sql`; idempotent
- `supabase/nft-merge-fixes.sql` — run LAST among the farm files (and re-run after re-running any of `farm.sql`/`farm-marketplace.sql`/`nft-leveling-rework.sql`, which carry superseded copies — they're marked with ⚠️ comments); idempotent. Fixes merge fallout: `farm_card_defs.minted_count` (monotonic minted-ever counter, incremented under the def row lock) now drives lootbox serials + edition supply + persona-name index — live-instance counts break once merges burn cards (serial UNIQUE collisions abort the whole box; sold-out editions silently reopen). Also makes `farm_nft_transfers.instance_id` nullable `ON DELETE SET NULL` (was `CASCADE`, which erased a burned card's entire provenance including the fresh `merge_fuel` row), rewrites `settle_marketplace_listing` to void (refund winner + release tile) instead of raising forever when a farm-tile auction winner has unpaid land-tax debt, and drops the ambiguous 3-arg `plant_crop` overload. Frontend mirrors: `farmCatalogModel`'s `minted` map and `openFarmNftExplorer` prefer `def.minted_count` (burned serials show 🔥, merge history rows render `merge_fuel`/`merge_hero`)
- `supabase/farm-bulk-lootbox.sql` — run after the farm/NFT files above; idempotent. Adds `open_farm_lootboxes(p_qty)` (cap **20**, mirror `FARM_BOX_BULK_OPEN_MAX` in `index.html`): locks the caller's `farm_user_state` row, then **loops the canonical single-box `open_farm_lootbox()`** in one transaction (so it stays correct whichever superseded copy is live, and per-box anti-hoarding/voucher/starter rules advance box by box), returning `packs:[{cards,got_voucher}]` + batch totals. Frontend: both buy sites (`renderFarmLootboxStore` in the Sklep, `buildFarmBoxesBody` in the hub) use a shared `farmQtyStepper`/`farmBoxBuyControls` qty picker (buy cap 50 = `FARM_BOX_BULK_BUY_MAX`, mirrors `buy_farm_lootbox`'s existing `p_qty` cap; max also clamped to what the wallet affords); `openFarmLootbox(btn, qty)` routes qty>1 to the bulk RPC and plays `playFarmMultiPackOpening` — a Hearthstone-style sequential reveal (shaking sealed box with spinning rays → burst flash → auto-flipping cards via the shared `farmPackCardEl` → running loot-chip tray → aggregated ×count summary grid, skippable with ⏩).
- `supabase/farm-goldbox.sql` — run after `farm.sql` + `nft-merge-fixes.sql` (re-run after re-running `nft-merge-fixes.sql`, as it copies the live `open_farm_lootbox` draw logic); idempotent. Adds the **second, premium** farm lootbox **„Złota Skrzynia" ⭐** (2026-07-12) as a **fully self-contained** feature — it never touches the standard box's `boxes` column or `buy_farm_lootbox`/`open_farm_lootbox`/`open_farm_lootboxes`. Adds a `boxes_gold` counter on `farm_user_state` and `buy_farm_goldbox` (BURN **500**/box, reason `farm_goldbox_buy`, cap 50) / `open_farm_goldbox` / `open_farm_goldboxes` (bulk, cap 20). `open_farm_goldbox` is a verbatim copy of the live `open_farm_lootbox` with exactly 7 deltas: **5** draws (`FARM_GOLDBOX_DRAWS`) not 3, voucher base **0.15** not 0.07, consumes `boxes_gold` (`no_goldbox` error), NFT edition weight decays by `power(2,owned_nfts)` not 3 (voucher territory divisor stays base 3), a **guaranteed rare+ floor** (first successful draw forced to `rarity IN ('rare','epic','legendary')` whenever any is drawable, via `v_rare_only`/`v_rare_eligible`), the starter-gift block removed (gold boxes never read/write `starter_opens_left`/`guaranteed_voucher`), and returns `boxes_gold`. All NFT mint/serial/persona/provenance logic is preserved. Value per unopened gold box = **500** in net worth (`economy-stats.sql` `farm_assets` + `leaderboard-net-worth-items.sql` `farm_boxes`, both re-run alongside; `farm_goldbox_buy` added to the burn-reason list). Frontend: `FARM_GOLDBOX_PRICE`(500)/`FARM_GOLDBOX_DRAWS`(5)/`FARM_GOLDBOX_BULK_BUY_MAX`(50)/`FARM_GOLDBOX_BULK_OPEN_MAX`(20) mirror the SQL; `fmGoldBoxes` state (wired into every `farm_user_state` select + `applyFarmUserState`); `farmGoldBoxCardEl`/`farmGoldBoxBuyControls`/`buyFarmGoldbox`/`openFarmGoldbox` clone the standard-box helpers routed at the gold RPCs, reusing `playFarmMultiPackOpening`/`playFarmPackOpening`/`farmPackCardEl` unchanged; a `.lootbox-card.is-gold` accent + a second card in both `renderFarmLootboxStore` (Sklep) and `buildFarmBoxesBody` (hub 📦 Skrzynki).
- `supabase/farm-seasonal-contracts.sql` — run after the farm/NFT files above. Adds weekly farm crop contracts starting **2026-07-06** with Marchewka, dynamic fair-cap-based bar targets, per-unit contract premium, top-3 payouts (2500+NFT / 1500+5 boxes / 1000+2 boxes), contributor bar payout (5 boxes for anyone with >=1 eligible unit), views for the 🏆 Wyzwanie hub, a **Monday 00:00 Europe/Warsaw** award cron, and a `sell_crop_to_npc` override that records eligible current-week harvested lots. Keep its sale-return JSON + event premium preview in `index.html` (`farmSellQuote`/`farmSeasonalEligibleQty`) aligned.
- `supabase/documents.sql` — „Moje Dokumenty": in-game document-signing archive. Adds `document_signatures` (own-row SELECT, PK `(user_id, doc_id)`, stores a drawn-signature PNG data-URL) + the idempotent `sign_document(p_doc_id, p_version, p_signature)` SECURITY DEFINER RPC (validates `data:image/` prefix + ≤300000 chars, upserts on version bump). Document **text** lives in `index.html` (`DOCUMENTS` constant); the DB stores only the signature. Frontend: `DOCUMENTS`/`mySignatures`/`loadMySignatures`/`needsSigning`, `setupSignatureCanvas` (pointer-drawn signature), `openDocumentModal`/`closeDocumentModal`/`signDocumentRpc`, the **📄 Moje Dokumenty** header button (next to 💼 Portfel) → `openMyDocuments`, and `maybeGateFarmDocument()` called from `loadFarm()`. A `gate:'farm'` mandatory doc (`farm_reform_2026`: the land-reform „ustawa" announcing P2P działka trading + the kataster land tax, styled as a one-page A4 sheet) shows a **blocking** memo on Ogródek open for **all users** — must scroll to the bottom **and** draw a signature to dismiss; re-appears each visit until signed (and after a `version` bump). The 📄 Moje Dokumenty button archives signed docs for everyone.
- `supabase/remove-heroes.sql` — idempotent; **the cosmetic heroes system is REMOVED** (2026-07): drops `user_heroes` + `save_hero()` + its realtime publication, and flips tavern-only cosmetic items to `sale_type='auction'` (owners keep them; no longer sold in the shop). **Hero ITEMS stay fully functional**, now renamed **„Przedmioty specjalne"** in the UI: `hero_item_defs`/`hero_item_instances`/`hero_item_auctions`/`hero_item_auction_bids`, the purchase/bid/settle RPCs, the `hero_item_auction_cards` view, and the `hero_items` valuation in `economy-stats.sql`/`leaderboard-net-worth-items.sql` are all untouched. Bought in the Sklep (`#hero-shop-section`, `renderHeroShopGrid`) or won at 🔨 auctions (`#hero-auctions-section`); owned items list in the Ogródek hub's 🎒 Mój Majątek tab (`buildSpecialItemsSection` inside `buildFarmInventoryBody`). Run AFTER deploying the frontend that removed the ⚔️ Herosi tab.
- `supabase/hero-items-always-active.sql` — idempotent; run after `hero-items.sql` (and after `remove-heroes.sql`). **Items no longer need equipping — owning one is enough for its effect to apply** (2026-07 product decision: "if you have it, you have it['s] ability"). Drops `hero_equipment` + `equip_hero_item`/`unequip_hero_item` + `public_hero_equipment` (equip's only reader was the now-gone tavern), and repoints every consumer at ownership: `my_hero_inventory` (drops the `equipped` column), `hero_score_bonus` (`hero-leaderboard-bonus.sql`, now joins `hero_item_instances` directly), and `award_daily_interest()` (`new-auction-items.sql`'s `interest_ring` cron, now pays every owner). **The 10 Edge Functions' shared `getStrongestHeroEffect(tx, userId, game)` pattern** (`whack-boss-action`, `bug-jumper-action`, `flappy-pants-action`, `snake-action`, `invoice-horde-action`, `var-patrol-action`, `egg-catch-action`, `roulette-action`, `slots-action`, `poker-action` — the last also has a standalone `poker_glasses` ownership check) all query `hero_item_instances`/`hero_item_defs` directly by `owner_id`, no `hero_equipment` join — keep any future copy of this pattern the same way. The three superseded files (`hero-items.sql`, `hero-leaderboard-bonus.sql`, `new-auction-items.sql`) carry ⚠️ comments; re-run `hero-items-always-active.sql` after any of them.
- `supabase/casino-luck-item.sql` — idempotent; run after `hero-items.sql` + `hero-items-always-active.sql` (and re-run after re-running either — supersedes their copies of `purchase_hero_item`, `my_hero_inventory`, `hero_score_bonus`, all marked ⚠️). Adds **timed shop items** (`hero_item_defs.duration_hours` + `hero_item_instances.expires_at`) and the first one: **„Amulet Bezwstydnego Fartu" 🍀** (10000🪙, 7 days; formerly „Amulet Fortuny") — a **COMMUNAL** `effect_game='casino'`/`effect_type='casino_luck'` buff: while ANY unexpired instance exists (any owner — the buyer pays for the whole office), EVERY player gets the boost, applied server-side by **5** Edge Functions: `slots-action` G6 weight +2 (RTP 89.1%→97.2%; shift 3 crosses 100%), `roulette-action` +1% on winning payouts (97.3%→98.3%; 2% would make the straight+`lucky_trousers`+amulet stack +EV), `plinko-action` payouts ×1.01 (richest config 98.13%→99.1%), `mines-action` house factor 0.95→0.98 (state returns the caller's effective `houseFactor` + `casinoLuck`), and `crash-action` cash-out wins ×`CASINO_LUCK_CRASH_FACTOR` (1.03) — the amulet is deliberately marketed as OVERPOWERED, so unlike the other four it is allowed to push a game **above 100% RTP**. **Roulette rescue rework (2026-07-12, same file + `roulette-action`):** the `win_chance_bonus` rescue (`lucky_trousers`) used to pay full multipliers on the rescued number, making straight-number betting +32% EV; it now pays a **stake refund** (rescued win capped at the round's total stake), the trigger chance is clamped server-side to `MAX_RESCUE_CHANCE_PCT` (1%) regardless of `effect_value`, and both bonus roundings (`payout_bonus`, amulet) are `Math.floor` not `Math.ceil` (ceil was +50% EV on 1-coin even-money wins). Worst allowed stack (straight + trousers + amulet) = 99.25% RTP — rerun the joint EV math before touching any of these constants. **Rakieta (crash) is now INCLUDED (2026-07-12)** — `crash-action`'s own `hasCasinoLuck(tx)` multiplies each cash-out win by `CASINO_LUCK_CRASH_FACTOR` (1.03); combined with the cash-out grace contract this is knowingly +EV, which is the intended "overpowered" flavor. Only poker stays excluded (PvP). Each purchase **queues +7 days onto the shared window** (buyer's new instance starts at `MAX(expires_at)` of all instances, def row locked FOR UPDATE against concurrent buys; RPC returns `extended`); the public `casino_luck_status` definer view exposes only the aggregate `active_until` (instances stay own-row RLS); `my_hero_inventory` hides expired instances and exposes `expires_at`/`duration_hours`; a daily `pg_cron` job (`hero_item_expiry_cleanup`, 03:20 UTC) deletes expired instances, which is also what removes them from net worth (`user_assets_value` counts owned instances at def price). The 5 functions' luck queries are ownerless (`hasCasinoLuck(tx)`/`casinoLuckFactor(tx)`) and filter `expires_at`; `getStrongestHeroEffect` also filters `expires_at`; frontend: `casinoLuckGlobalUntil` (loaded in `loadHeroItemCatalog`), `heroItemExpiryLabel`, the timed-item branch in `renderHeroShopGrid` (⏳ shared countdown + "Przedłuż" button), expiry chip in `farmSpecialItemTile`, `casino` case in `heroItemEffectLabel`.
- `supabase/hide-legacy-hero-items.sql` — idempotent; run after `hero-items.sql`. Flips `sale_type` to `'hidden'` for six early low-impact items (`dealer_hat`, `luck_brooch`, `reflex_gloves`, `jumper_boots`, `bluff_dagger`, `lucky_trousers`) so they drop out of `renderHeroShopGrid()`'s catalog query (`.in('sale_type', ['shop','both'])`) without deactivating them — `is_active` stays `true`, so existing owners keep the effect and still see the item in `my_hero_inventory`/Mój Majątek (own-row, no `sale_type` filter). `hero-items.sql`'s own copies of these six rows are also flipped to `'hidden'` (⚠️ comment added) so a future re-run doesn't resurrect them in the shop.
- `supabase/lottery.sql` — „Loteria po Mundialu": an activity-based raffle on its **own nav tab** — `#tab-lottery` (`data-tab="lottery"`, a „Nowość"-badged entry in the „🎯 Zakłady" dropdown next to Rynki + Mundial; `switchTab('lottery')` shows `#tab-lottery` and calls `renderLottery()`). Adds the read-only `mundial_lottery_standings()` SECURITY DEFINER RPC (idempotent, granted to anon+authenticated) that returns the live prize pool + per-player ticket standings as JSON. **Prize pool = 10 × the Bank's net profit from settled Mundial bets** (`GREATEST(0, Σ lost stakes − won payouts) × 10`, mirrors `computeFootballHouseStats().realizedProfit`). **Tickets are earned across the WHOLE portal** with per-source caps (kept in sync with `LOTTERY_EARN`/the caps in `index.html`): Mundial settled bets 1/bet cap 15, prediction-market `trades` 1/bet cap 10, casino 1/day-played cap 10 (union of `plinko/mines/crash/wheel/roulette/slots_spins`), seasonal 1/week-scored cap 8 (union of the 8 `*_scores`), farm 1/active-day cap 8 (`farm_seasonal_event_sales`+`farm_inventory`), marketplace 1/settled-trade cap 6, canvas 1/day-painted cap 6; **three ownership rewards** — Ogród Zen (+2 for ≥1 zen plant, +3 more for a 2nd, cap 5, from `gardens` row count per user), Ozdoby (+2 per plant whose `gardens.equipped` jsonb is non-empty — a plant currently WEARING an ozdoba, NOT per owned accessory, cap 4 so max 2 plants), Działki (+2 per owned non-migration `farm_tiles` row, cap 12); a **breadth bonus** (+2 per of the 7 *activity* categories touched, max +14 — ownership rewards do NOT count toward breadth) to promote trying many activities; and a +1 welcome ticket. **Admin accounts (`is_admin`) are excluded.** All day-bucketing uses Europe/Warsaw. The RPC returns `player_count` + a per-player row carrying every category field. Points collected **until 31 July 2026**, draw **1 August** (dates hardcoded in the RPC + copy). The actual weighted draw/payout is NOT built yet — this ships only the live standings + rules panel. Frontend: `renderLottery()`/`buildLotteryPanel()` (renders into `#lottery-body`; caches `lotteryData`), a master `LOTTERY_CATS` list (each category's `icon`/`label`/`cap`/`how`-to-earn text, split via `LOTTERY_ACTIVITY_KEYS`/`LOTTERY_OWNER_KEYS`) drives the grouped „Za co zdobywasz bilety" catalog, the **compact „Twoje bilety" personal breakdown** (pinned at the **very bottom** of the panel — the logged-in user's one-line-per-category points/cap plus a „+N <how>" hint or „✓ komplet", so players know precisely what to grow), AND the **Ranking view toggle** (`lotteryRankView` `'list'|'matrix'` + `setLotteryRankView()` which re-renders from cache): **Lista** = per-player rows with gold share-bars; **Macierz** = a full players×categories matrix (icon column headers with `title` tooltips, Σ total column, own row highlighted, zeros dimmed / capped cells gold) so everyone can compare all breakdowns. `LOTTERY_TIERS` (6 tiered winners 30/20/14/10/8/6% of pool, drawn weighted without replacement) + `LOTTERY_DIVIDEND_PCT` (12% split equally among all `player_count` ticket holders — the split section spells out the exact formula) + a lottery-only `lotNum()` (Polish thousands grouping). **Minimalist design** grounded in a raffle-ticket motif (perforated ticket-stub hero with CSS notch cut-outs, hairline-ruled sections, a single gold accent `--lot-gold` used only for money/leader/your-row); CSS under `.lot`/`.lot-*` (built for the app's light theme).
- `supabase/prod-hardening.sql` — idempotent; adds indexes and tightens permissions; safe to re-run
- `supabase/reset-data.sql` — wipes all markets, trades, and poker state, resets every profile to 1000 coins

## Architecture

Everything lives in `index.html`: HTML structure, all CSS (CSS variables for theming), and all JavaScript in one `<script>` block. No bundler, no framework, no TypeScript.

**Supabase client** is loaded from CDN. The anon key is intentionally public — Supabase RLS and function grants are the security boundary.

### Key global state

| Variable | Contents |
|---|---|
| `me` | Logged-in user profile (`id`, `nick`, `coins`) |
| `markets[]` | All market rows from DB |
| `tradesByMarket{}` | All individual trade rows, keyed by `market_id` |
| `myPositionsByMarket{}` | Current user's aggregated positions (from `positions` view) |
| `pokerState` | Sanitized shared poker table state returned by the Edge Function |
| `whackBossRuntime` | Current local Whack-a-Boss round timing, clicks, and score |

### Data flow

1. `loadMarkets()` fetches markets, trades, and profiles in parallel then calls `renderMarkets()`.
2. `renderMarkets()` fetches the current user's positions (`positions` view), then calls `buildMarketCard()` for each market.
3. Realtime subscriptions on `markets` and `trades` tables update state and call targeted update helpers (`updateCardProb`, `updateCardChart`) instead of full re-renders where possible.

### Auth

Nick + 5-digit PIN. Implemented as Supabase email/password auth using the synthetic email `nick@typuje.local`. A Postgres trigger (`handle_new_user`) creates the `profiles` row on first signup. Economy writes go through RPCs or Edge Functions; the admin-only garden stage/streak controls use column-limited direct updates protected by RLS.

### CPMM pricing

`p(YES) = no_shares / (yes_shares + no_shares)`. Markets start at `yes_shares = no_shares = 500` (50%). Constant product `k = yes_shares × no_shares` is maintained by `place_bet`. Shares out for a NO bet of X coins:

```
dN = (N × X) / (Y + X)
sharesOut = X + dN
```

### Payout formula

Resolution distributes the total pot via the RPC `resolve_market`. The formula (mirrored in `estimatePayout()` for the live UI estimate):

```
payout = user_amount_bet + losing_side_pot × (user_shares / total_winning_shares)
```

Winners always get at least their bet back; the losing side's coins are split proportionally by share count. The estimate and the actual SQL resolution use identical math.

### DOM helper

`el(tag, attrs, ...children)` creates DOM nodes. **Children must be strings or Node objects — never arrays.** When you need to mix text and multiple element nodes outside of `el()`, use `node.append(str, node, str, ...)` instead.

### Permissions

All mutations require authentication and go through Supabase RPCs:
- `place_bet(market_uuid, side, amount)` — enforces coin balance, side-locking (can't bet both sides on the same market), and CPMM math atomically
- `create_market(icon, title, deadline)`
- `resolve_market(market_uuid, resolution)` — only market creator or a profile with `is_admin = true` can call this

Users can only add to one side per market (side-locked after first bet). Profiles with `is_admin = true` can resolve any market.

### Marketplace (Targowisko)

The Sklep tab contains a peer-to-peer marketplace ("Targowisko 🛍️") where **any logged-in user** can list IRL goods or services for coins. Unlike the admin-only store rewards and hero-item auctions (where coins are burned), marketplace coins **transfer from buyer to seller**. Two listing formats are supported: fixed-price (`listing_type='fixed'`, instant buy) and timed auction (`listing_type='auction'`, bidding with escrow). The bidding engine mirrors `place_hero_item_bid`: coins are escrowed on each bid, the previous leader is auto-refunded when outbid, and the leading bidder only pays the incremental difference to top up their own bid. Settlement (`settle_marketplace_listing`) credits the escrowed winning amount to `seller_id`. Sellers (or admin) can settle their own auction; sellers can cancel an open listing with no bids. All RPCs are in `supabase/marketplace.sql`; the `marketplace_cards` view is used for card rendering.

### Poker

Poker is one shared authenticated Texas Hold'em table. Browser clients call `sb.functions.invoke('poker-action', ...)`; they do not write poker tables directly. The Edge Function verifies the user JWT, uses `SUPABASE_DB_URL` for a Postgres transaction, and stores hidden cards/deck in service-only tables. Public realtime updates on `poker_tables`, `poker_seats`, and `poker_events` only trigger a sanitized state reload.

Table defaults are 100 coin buy-in, 1/2 blinds, 6 seats, and a 30 second action timer. Sitting deducts the buy-in from `profiles.coins`; standing is allowed only between hands and returns the remaining stack. The leaderboard view includes active poker stacks so seated players keep their net worth while playing.

### Roulette (Ruletka)

Roulette is one shared authenticated table (`supabase/roulette.sql` + the `roulette-action` Edge Function; `verify_jwt` off, JWT checked per action). Up to 6 players sit in `roulette_seats`; coins are deducted **only at spin/resolve time** (`resolveRound`), never at bet time — so dropping a seat's open bets refunds nothing. The frontend (`#tab-roulette` → `loadRouletteState`/`renderRoulette*`) is a CSS-grid rounded-rect felt: a control dock on top, wheel + number board side-by-side, an on-felt seat strip, and a lower band holding the bets list + recent results. The unified button system is `.rl-btn` (`is-primary`/`is-ghost`/`is-gold`); the round chip selector reuses the shared `.casino-chip` (do not edit that base for roulette-only tweaks — scope under `.roulette-chips`).

**Idle-seat auto-eviction:** abandoned (closed-tab) seats are freed poker-style — `getState` runs `evictStaleSeats` on every read, deleting any seat whose `last_seen` is older than `SEAT_STALE_MS` (90s, server-only constant in `roulette-action`) and bumping the caller's own `last_seen`. The browser pings `state` every `ROULETTE_HEARTBEAT_MS` (25s, in `index.html`) while on the tab + visible + seated, plus on `visibilitychange`. The two constants are coupled: keep the client heartbeat ≤ ⅓ of the server threshold. `last_seen` lives on `roulette_seats` (idempotent `ADD COLUMN IF NOT EXISTS`).

### Plinko G6

Plinko G6 is a solo authenticated house game (`supabase/plinko.sql` + `plinko-action`). The browser calls the Edge Function directly with the active Supabase access token for `state`, `drop`, and queued `drop_batch` requests; it never writes tables directly and never chooses the path. The Edge Function validates stake/rows/risk, locks `profiles.coins`, generates crypto-random left/right paths, writes `plinko_spins`, updates the balance, and returns completed drops. The frontend (`#tab-plinko`) is an Office Arcade canvas board: glowing pegs, G6 ball trail, bucket bursts, paper/calculator/coffee particles, reduced-motion fallbacks, top-positioned start button, timed requests, and rapid-click queue playback. The payout tables live in both `index.html` (`PLINKO_PAYOUTS`) and `plinko-action`; keep them in sync, and re-run `hazard-views.sql`, `coin-inflow-stats.sql`, and `economy-stats.sql` after installing.

### Miny G6

Miny G6 is a single-player authenticated casino game (`supabase/mines.sql` + `mines-action`). The browser calls `sb.functions.invoke('mines-action', ...)` for `state`, `start`, `reveal`, and `cash_out`; it never writes tables directly and never receives mine positions while a round is active. A player can have only one active `mines_rounds` row; `mines_round_secrets` stores the hidden mine tiles with no client grants; completed rounds are copied to `mines_spins` for history, Hazardista, coin-inflow, and economy house-net stats. The 5×5 board size, 95% house factor, and multiplier cap live in both `index.html` and `mines-action`; keep them in sync.

### Whack-a-Boss

Whack-a-Boss is an 18-second authenticated mini-game. The browser calls `sb.functions.invoke('whack-boss-action', ...)`; the Edge Function creates the round schedule, validates submitted click timing/positions, stores scores, and returns weekly/all-time leaderboards. Weekly awards are paid by `award_whack_boss_week()` through `pg_cron`: rank 1 gets 500 coins, rank 2 gets 250, rank 3 gets 100. Weekly leaderboards are date-filtered by ISO week in the Europe/Warsaw timezone; all-time records stay stored separately.

### Seasonal games

The seasonal tab hosts one rotating arcade game per Monday-start week (Europe/Warsaw). `SEASONAL_ANCHOR_WEEK_START` plus `SEASONAL_ROTATION` in `index.html` derive the active game for any future week; `SEASONAL_OVERRIDES` can replace a specific week; `getCurrentSeasonalEntry()` picks the active game and `loadSeasonalTab()` shows the matching `seasonal-game-*` panel. Current rotation: `whack_boss` → `bug_jumper` → `flappy_pants` („3 Pary Spodni") → `snake` → `invoice_horde` („Najazd Ticketów") → `var_patrol` (VAR Patrol) → `egg_catch` („Łap Jajka") → `super_mariusz` („Super Mariusz") → `popup_panic` („Zamknij Popupy!"), with Snake overriding the week starting 2026-06-15, VAR Patrol overriding the week starting 2026-06-22, Najazd Ticketów overriding the week starting 2026-06-29, Łap Jajka debuting via override on the week starting 2026-07-06, Super Mariusz debuting via override on the week starting 2026-07-13, and Zamknij Popupy! debuting via override on the week starting 2026-07-20.

Each seasonal game mirrors the same stack:
- `<game>_rounds` / `<game>_scores` / `<game>_weekly_awards` tables, `<game>_current_week` / `_all_time` / `_recent_awards` views, an `award_<game>_week()` SECURITY DEFINER payout (500/250/100), realtime publication, and a `pg_cron` job at `'5 0 * * 1'` (Monday 00:05 UTC) that pays the previous week's top 3. The cron jobs are season-gated by `seasonal_game_for_week()` (`supabase/season-award-gating.sql`) so off-season rounds never trigger a payout — **that SQL function mirrors `SEASONAL_ANCHOR_WEEK_START`/`SEASONAL_ROTATION`/`SEASONAL_OVERRIDES` from `index.html` and must be updated and re-run whenever the rotation or overrides change**.
- An Edge Function (`<game>-action`) with `state`/`start`/`submit` actions. The browser cannot write score tables (RLS grants SELECT only); the function owns inserts via `SUPABASE_DB_URL`, caps the per-round score, enforces the round expiry window, and applies hero score bonuses only for games that support them.
- Frontend: a canvas runtime (`new<Game>Runtime`/`<g>Draw`/RAF loop) plus `invoke*`/`load*`/`render*` helpers, wired into `loadSeasonalTab()`, the realtime subscriptions, and the `loadSeasonHistory()` recent-awards aggregator.

Bug Jumper hard course v2 uses `course_id = 'bug_jumper_hard_v2'`: a fixed 10-column, 30-line course shared by every player, no per-round random lane setup, safe rest lines at 10/20/30, one point per line reached, server replay of the submitted movement log, `completion_ms` tie-breaks, and hard-course-only leaderboard/award views. Existing legacy rows remain stored as `legacy_random_v1`.

„3 Pary Spodni" (Flappy Pants) is a Flappy Bird clone: you ARE a pair of trousers (Space/click/↑ to flap) with 3 lives (the "3 pary spodni"); each crash costs one pair with brief invincibility, and the round ends after the third. Score = obstacles passed; it persists across lives.

Snake is a 20x20 seeded-grid seasonal game prompted by Filip with `do snake, make no mistakes`. The browser submits direction changes, and `snake-action` replays them server-side to determine apples eaten before wall/self collision or the 120-second cap. Weekly ranking uses score descending, then shorter `duration_ms`, then earlier submission.

„Najazd Ticketów" (internal gameType `invoice_horde`) is an original IT-helpdesk arena-survivor (inspired by the Vampire-Survivors / Devil-Daggers genre, written from scratch — not copied). You run an IT guy 🧑‍💻 (8-way WASD/arrows/swipe) dodging an endless swarm of support tickets 🎫 that home in from the edges; an auto-firing „ROZWIĄZANE" script hitscans the nearest ticket in range (+1 resolved). Every ~125 ticks (~10 s) a **„CF" mini-boss** spawns — an oversized, slower (`BOSS_SPEED = 4`), bigger-hitbox ticket that soaks `BOSS_HP = 5` script hits before dying for `BOSS_SCORE = 5`; it lives in the same `enemies` array flagged `boss:true`, so focusing it lets the swarm pile up (risk/reward). Its 👔/"CF"/HP-bar rendering is client-only cosmetic; its constants are part of the byte-for-byte parity contract. **Survival mode, one life:** a single touch ends the run — there is no countdown, the spawn rate ramps until it outpaces the script so standing still gets you surrounded (you must kite the swarm into a trailing line). `ROUND_DURATION_MS` (60 s) is only a hard replay/safety cap; the swarm is capped at `ENEMY_CAP = 70`. Like Snake it is a **deterministic integer simulation on an 80 ms tick** (seeded LCG enemy spawns, `isqrt`-based homing, no floats that desync) — the browser logs only input-direction changes `{tick,dir}`, and `invoice-horde-action` replays `seed + moves` to derive the trusted kill count (anti-cheat ceiling `MAX_SCORE_PER_ROUND = 200`). The client renders at 60 fps by interpolating between sim ticks, with canvas particle/`+1`/death-shake effects that are purely cosmetic (never read by the sim or submitted). Weekly ranking: score (tickets resolved) descending, then **longer** `duration_ms` (survival time), then earlier submission. The constant block in `index.html` (IH_*) and `invoice-horde-action` must stay byte-for-byte equivalent — there is a headless parity harness pattern (client tick-sim vs server replay) used to verify 0 mismatches whenever they change.

VAR Patrol (internal gameType `var_patrol`) is a rapid football-officiating judgement game. The Edge Function issues a server-owned schedule of offside/foul/handball/goalline scenarios, strips the authoritative answer before returning it, and validates submitted `{index, answer, reactionMs}` decisions against the schedule with a 120 ms reaction floor and shrinking decision window. The browser renders the visible scene and two verdict buttons; local score/accuracy/combo are cosmetic, while `var-patrol-action` stores the trusted score and leaderboards. Weekly ranking uses score descending, accuracy descending, then earlier submission.

„Łap Jajka" (internal gameType `egg_catch`) is a Nu-pogodi/„Elektronika"-style LCD egg catcher: eggs roll down 4 chutes from hens at the top, and a wolf 🐺 catches them by moving between 4 basket positions (Q/A/P/L, arrow keys, or tapping a screen quadrant); 3 broken eggs (missed catches) end the run, and speed ramps up every 10 eggs spawned (`EC_LEVEL_EVERY`). Like Snake/Invoice Horde it is a **deterministic integer simulation on a 100 ms tick** (seeded LCG spawns via `ecRng`/`ecAdvanceTick`) — the browser logs only wolf-position changes `{tick,pos}`, and `egg-catch-action` replays `seed + moves` to derive the trusted catch count (anti-cheat ceiling `EC_MAX_SCORE = 1000`, `EC_MAX_TICKS = 6000` = 10 min hard cap). The constant block in `index.html` (`EC_*`) and `egg-catch-action` must stay byte-for-byte equivalent for the simulation transition functions (rendering-only constants like `EC_CS_W`/`EC_MAX_DPR` are client-only). Weekly ranking uses score descending, then earlier submission.

„Super Mariusz" (internal gameType `super_mariusz`) is a Mario-style HARDMODE **one-life** speedrun platformer on a fixed shared office-corridor course (`SM_COURSE`, `course_id = 'super_mariusz_v2'` — no per-round randomness). Touching a spike, a patrolling enemy 💼, or falling into a pit kills instantly; ranking is **progress first, time second** (2026-07-10 change — most players never finish, so distance must count). The stored `score` is the **progress in tile columns** reached by the player's center (`smProgressScore`: peak `maxX`, capped at the flag column = 455), so the existing `ORDER BY score DESC, completion_ms ASC NULLS LAST` machinery ranks distance first; every finisher scores exactly 455 and competes on `completion_ms ASC` (`smFormatTime()` renders it as `m:ss.cs`, shown with a 🏁 prefix), while DNF rows display the progress percent (`smProgressPct`). A death or hitting the 5-minute cap (`SM_MAX_TICKS = 6000`) is a **DNF**: `completed = false`, `completion_ms = NULL`, but the progress score still counts and DNFs CAN win weekly awards (`award_super_mariusz_week` filters `score > 0`, which any run satisfies — the start column already scores 2). Like Snake/Invoice Horde/Łap Jajka it is a **deterministic integer simulation on a 50 ms tick** (fixed-point physics, 256 sub-units/tile, no floats) — the browser logs only held-key bitmask changes `{tick,keys}` (bit0 LEFT, bit1 RIGHT, bit2 JUMP) via `smAdvanceTick`, and `super-mariusz-action` replays the course + moves to derive the trusted result (anti-cheat: submitted `elapsedTicks` capped against wall-clock elapsed +1.5s tolerance). Hero `score_bonus` items apply as a **time bonus, not points** (2026-07-13, per Kornel's request): progress can't be boosted (it would credit tiles never reached), so `super-mariusz-action` subtracts `SM_ITEM_TIME_BONUS_MS_PER_POINT` (400 ms) × `effect_value` from a **FINISHED** run's `completion_ms` (e.g. `kaiser_helm` +5 → −2.0 s; DNFs unaffected), recording `item_effect` (`bonus_ms`, `base_completion_ms`) in `client_meta`; the cross-game `getStrongestHeroEffect` list in that function includes `egg_catch`+`super_mariusz` and filters `expires_at`. **`super_mariusz_v2` (2026-07-09) is a brutal ~460-column, ~66s-optimal-clean-run course** — spike gauntlets, 4-5 tile max-distance pit jumps, a floating-platform section over a bottomless pit, patrolling-enemy ledges (`SM_ENEMY_SPEED` raised 24→32), a 2-row ceiling squeeze, and a final combined gauntlet — replacing the original `super_mariusz_v1` test course (27s clean run, too easy). The `SM_*` constant block, `SM_COURSE`, and the `smParseCourse`/`smInitState`/`smAdvanceTick`/`smProgressScore` transition functions in `index.html` and `super-mariusz-action` must stay byte-for-byte equivalent (rendering-only constants like `SM_CS_W`/`SM_TILE_PX` are client-only); `scripts/sm-parity.mjs` fuzzes both copies against a golden completable run and course-geometry sanity checks. **`scripts/sm-solve.mjs`** is a beam-search solver (states keyed by `tick/x/y/vx/vy/onGround`, scored by x-progress) used to find/verify a completing `GOLDEN_MOVES` log whenever the course changes — run it after any course edit and paste the resulting move log + endTick/completionMs into `sm-parity.mjs`'s golden-run assertion (must land in the 60-90s band). The rendering is full NES/SMB1-pixel-art (`smDraw`/`smInitCanvas` in `index.html`, `imageSmoothingEnabled = false`, 8×8 virtual-pixel sprites for tiles/spikes/player/enemies/flag, deterministic parallax clouds/bushes, a scanline overlay, and `Press Start 2P` HUD font on the SM stat/status text) — purely client-only, no sound. Weekly ranking uses score descending (progress), then `completion_ms` ascending among finishers, then earlier submission.

„Zamknij Popupy!" (internal gameType `popup_panic`) is a fast **~30-second, one-run** popup-closing frenzy (debuts via override on the week starting 2026-07-20). Random error/spam/malware popups pile onto a fake office desktop; you click/tap each to close it. **Two lose conditions:** `PP_MAX_OPEN` (12) popups open **at once** = buried, and a red **malware** popup left open past its `PP_MALWARE_TICKS` (25-tick / 2.5 s) deadline = infected. Surviving to `PP_ROUND_TICKS` (300 ticks) wins. Score is weighted: normal popup `PP_SCORE_NORMAL` (+1), malware `PP_SCORE_MALWARE` (+3). Spawns ramp (`ppSpawnGap` shrinks from `PP_SPAWN_GAP_START` 8 to `PP_SPAWN_GAP_MIN` 2 every `PP_RAMP_EVERY` 30 ticks) and can come in 2-popup bursts after tick 120. Like Snake/Invoice Horde/Łap Jajka it is a **deterministic integer simulation on a 100 ms tick** (seeded LCG spawns via `ppRng`/`ppSpawnOne`) — the browser logs only the popup **ids it closed per tick** `{tick,id}`, and `popup-panic-action` replays `seed + events` to derive the trusted score (a close is only credited if that id was actually open and had materialized ≥`PP_MIN_REACTION_TICKS` after spawn). Anti-cheat ceilings: `PP_MAX_SCORE` 2000, `PP_MAX_EVENTS` 3000; `elapsedTicks` capped against wall-clock elapsed +1.5 s. Hero `score_bonus` items apply as a flat point bonus (the cross-game `getStrongestHeroEffect` list in that function includes `egg_catch`+`super_mariusz`+`popup_panic` and filters `i.expires_at`). The `PP_*` constant block and the `ppInitState`/`ppRng`/`ppSpawnGap`/`ppSpawnOne`/`ppAdvanceTick` transition functions in `index.html` and `popup-panic-action` must stay byte-for-byte equivalent (cosmetic constants — `PP_NORMAL_POPUPS`/`PP_MALWARE_POPUPS` message pools, board-position rendering — are client-only, derived from popup id, NOT the shared RNG); `scripts/pp-parity.mjs` fuzzes a realistic player-driver's event logs (greedy/malware-only/random/slow/none strategies, plus garbage-id injection) through both transcribed copies and asserts 0 mismatches across all three outcomes. Rendering is **DOM-based** (authentic Win95/XP-style popup windows with title bars, red ✕, and a draining deadline bar on malware; `pp-*` CSS classes on a `#pp-arena` fake desktop with a faux taskbar) — no canvas. Weekly ranking uses score descending, then earlier submission (`accuracy` stores survival %). Run order: `popup-panic.sql`, then re-run `season-award-gating.sql`.

### World Cup betting (Mundial)

The `⚽ Mundial` tab is **fixed-odds betting vs the house** on FIFA World Cup 2026 matches — unlike the peer-pooled CPMM markets, it mints/burns coins like slots/roulette. A player picks a match outcome (1 = home / X = draw / 2 = away); the real bookmaker decimal odds are **locked at bet time** and a winning bet pays `floor(stake × locked_odds)`. Losing stakes are burned. The displayed "szansa" per outcome is the **de-vigged implied probability** (`(1/odd_i) / Σ(1/odd_j)`).

Each user may place **at most one bet per match** (no top-ups, no opposite outcomes on the same fixture). The server enforces this inside the `placeBet` transaction; the frontend disables odds buttons once a bet exists for that match.

Events and odds come from **The Odds API v4** (`api.the-odds-api.com/v4`, sport key `soccer_fifa_world_cup`); **settlement results come from a second provider, football-data.org v4** (`api.football-data.org/v4`, competition code `WC`). Both are written **only** by the `football-action` Edge Function over its service `postgres` connection — the browser has SELECT-only access and **never sends odds** (the `bet` action reads the trusted odds already stored in `football_matches`). Match ids are The Odds API's hex event ids (text); football-data.org has its own numeric ids, so matches are looked up by team name instead (see below). The function is `verify_jwt = false` so it can serve the public `state` read and the cron path; the `bet` action validates the user JWT manually via `requireUser()`.

**Why two providers, and the 90-minute settlement rule:** the 1X2 market is standard betting convention — it settles on the **90-minute (regulation) result**, ignoring extra time and penalties, even for knockout matches. The Odds API's `/scores` only ever returns a match's actual final score with no way to tell whether it includes extra time, so a knockout match decided in ET would settle on the wrong side. football-data.org's fixtures expose `score.duration` (`REGULAR`/`EXTRA_TIME`/`PENALTY_SHOOTOUT`) plus a `score.regularTime` object whenever `duration` isn't `REGULAR`, which gives the true pre-ET score. `loadRegularTimeScores()` in `football-action` derives, per match: the **result score** (always regulation-time, drives `1`/`X`/`2`), the **display score** (real goals scored — `score.fullTime`, except for `PENALTY_SHOOTOUT` where `fullTime` bizarrely adds the shootout kicks on top of the goal count, e.g. a 1-1 draw decided 4-3 on pens is reported as `fullTime` 4-5, so `regularTime` is used for display too), and the stored `status` (`FT`/`AET`/`PEN`). This is explained to players in the "🥅" rule of the Mundial tab's "Jak działa Mundial?" panel. The Odds API and football-data.org spell a handful of teams differently (`DR Congo`/`Congo DR`, `Cape Verde`/`Cape Verde Islands`, `Czech Republic`/`Czechia`, `USA`/`United States`) — `canonicalTeam()`/`TEAM_ALIASES` in `football-action` normalize both sides before matching.

An **every-6h** `pg_cron` job (`football_hourly_sync`) `net.http_post`s the function with `{action:'cron'}` and the `x-cron-secret` header. The `cron` action: (1) **syncs events+odds** from The Odds API in one `/sports/{sport}/odds?regions=eu&markets=h2h&oddsFormat=decimal` call (the odds response carries fixtures, so no separate fixtures call) — picks a usable bookmaker per event, de-vigs the 1X2 prices, and upserts; (2) **settles** finished matches by pulling `/competitions/WC/matches?dateFrom=…&dateTo=…` from football-data.org (only spent when a match is still unsettled and past kickoff), deriving `1/X/2` from the regulation-time score and crediting winners' `potential_payout`; (3) **voids+refunds** stale matches (`voidStale`) — any match still `open` with no result **>12h past kickoff** (cancelled/abandoned/walkover/API gap/no team-name match) is marked `CANC`/`settled` and its open bets are refunded their stake and set to `void`. Void runs *after* settle so a genuine result always wins. An admin-only **`void`** action (`is_admin = true`, JWT-validated) does the same immediately for a specific `matchId` as a safety valve. **The Odds API free tier is 500 req/month, so the odds sync alone justifies the every-6h cadence (≤1 req/run ≈ 120/month) — football-data.org's free tier (10 req/min, no monthly cap) isn't the limiting factor.**

Config lives in Edge Function secrets: `ODDS_API_KEY` (the-odds-api.com key), `FOOTBALL_DATA_API_KEY` (football-data.org key), `FOOTBALL_CRON_SECRET` (shared with the cron block in `supabase/football.sql`), and optional `ODDS_SPORT_KEY` (default `soccer_fifa_world_cup`), `ODDS_REGIONS` (default `eu`), `ODDS_BOOKMAKER` (preferred bookmaker key, else first usable), `FOOTBALL_DATA_COMPETITION` (default `WC`). Frontend: `invokeFootball`/`loadFootball`/`renderFootball`/`buildMatchCard` plus a dedicated `#fb-overlay` bet modal, wired into `switchTab('football')` and the `football_matches`/`football_bets` realtime subscriptions. One-off manual corrections to already-settled matches (e.g. a match settled before the football-data.org switch landed) use `coin_transactions.reason = 'football_correction'`.

### Collaborative canvas (Wspólne Płótno)

The `🎨 Wspólne Płótno` tab is a shared **r/place-style pixel board** — one common **192×108** (16:9) grid that the whole office paints together, rendered near-fullscreen with a thin instructions/toolbar bar on top. Unlike a game, there is no score, and (2026-07-14) **every pixel is free for everyone, unconditionally** — the earlier 1-free-pixel-per-2h / pay-1-coin-on-cooldown split is gone (`supabase/canvas-free-for-all.sql` supersedes that RPC; the `canvas_cooldowns` table is left in place, just unused). A fixed ~16-color palette keeps the canvas coherent. The nav tab carries a small „Nowość" pill (`.nav-new-badge`, shared with the Targowisko nav button).

Unlike Mundial/poker, the write path is a **direct `SECURITY DEFINER` RPC** (`place_pixel(p_x, p_y, p_color)`), not an Edge Function — it mirrors the garden `water_plant()` cooldown pattern (minus the cooldown, now). The browser has SELECT-only access to `canvas_pixels`/`canvas_cooldowns` and calls `sb.rpc('place_pixel', …)`; the RPC validates bounds + hex color, then upserts the pixel `ON CONFLICT (x,y) DO UPDATE` (last-write-wins, so simultaneous clicks resolve to one final color).

The **grid bounds (192×108) are duplicated** between `supabase/canvas.sql` (the RPC) and `index.html` (`CANVAS_W`/`CANVAS_H`) — keep them in sync. Frontend: `loadCanvas`/`drawCanvas`/`placePixelRpc`/`onCanvasTap` + `cvSetup` pan/zoom pointer handling (`cv*` helpers), wired into `switchTab('canvas')` and a `canvas_pixels` realtime subscription (`applyCanvasPixelChange`) that patches single cells without a full reload. `fetchAllCanvasPixels` paginates the initial SELECT in deterministic `(x,y)` order because Supabase caps one Data API response at 1,000 rows; changes arriving during that multi-request load are replayed onto the completed snapshot so fresh pixels are not lost.

### Farma (farm grid + card economy)

**Farma** is exposed as the top-level nav tab labelled **🌱 Ogródek** (`#tab-farm`, `data-tab="farm"` → `switchTab('farm')` → `loadFarm()`); the original zen `#tab-garden` view is no longer visible in nav, and `switchTab('garden')` redirects to farm, but the old code remains for migrated plant tiles. **Ogródek / Farma** is a Stardew/Happy-Farm + collectible plant-card layer: a shared **13×4 DOM-grid field** rendered with the same Ogródek scene (grass + dynamic Wrocław sky band + weather; `applyGardenAtmosphere` also paints the farm via `applyFarmAtmosphere`). Players **own individual tiles**. Each existing gardens row is migrated onto one tile (linked by `zen_garden_id`) that shows a **scaled mini `buildPot`** of that plant; **clicking it opens the full interactive `buildPot(g, isMine)`** in the side panel — identical look/size/stats/feel, with the 💧 Podlej watering + cooldown (crops can't be planted on a plant tile → `zen_tile`). `scaleFarmPots` sizes the board to square cells filling the field and sets `--pot-scale`. The farming loop on bought tiles: **buy a tile** (doubling BURN: 350→700→1400→… capped at 50000, vouchers still 0) → **open a 100🪙 loot box in the Sklep tab** (`open_farm_lootbox`; the farm only links there via `openShopSection('farm-lootbox-section')`) to draw **3 distinct** rarity-weighted plant **cards** at once (NFT weights are divided by 3 per owned NFT; voucher chance is divided by 3 per owned tile/voucher), revealed with a theatrical pack opening (`playFarmPackOpening` — staggered deal-in + per-card 3D flip + rarity glow) → stack duplicates and **level a card** (`level_up_card`, consumes `2×level` dupes + `50×level²` coins, effectively infinite upgrades; **NFT species level via `level_up_nft` merge instead** — see Phase 2) → **plant** a card on an owned empty tile (`plant_crop(p_x,p_y,p_species,p_instance_id)` — the instance id picks WHICH numbered NFT is planted, NULL for fungible cards; growth time = `max(1440, base_grow_minutes × 0.92^(level-1))` minutes — base times are 1 day/2 days/3-4 days by rarity and a hard **24h floor** means harvests happen at most once a day regardless of level; server-owned `ready_at`) → **harvest** (`harvest_crop`, mints `base_yield × (1+(level-1)×0.5)` crop units into `farm_inventory` as a **per-harvest lot that rots 5 days later** — crops minted, not coins) → **sell to the NPC** (`sell_crop_to_npc`, MINTS coins, consumes lots **FIFO by soonest expiry**) at a **fluctuating "stalk market" price**: the catalog shows the **MAX** (`base_price`); the NPC pays the live `cur_price`, which is re-rolled to a new **anchor** (30–100% of MAX, Balanced avg ≈0.57×) **twice daily at 00:00 & 12:00 Europe/Warsaw** (`roll_farm_prices`). A sale dips `cur_price` **once per transaction scaled by size** (`min(40%, 0.5%×qty)`, batch charged the pre/post average, floor 30% of MAX), then it **recovers toward the day's anchor at ~12%/hr** — so players time sales to the swing (read the 7-day chart) and dumping a big haul craters the price. Unsold crops rot (`farm_rot_cleanup` daily cron); every sale is shown in the public 🧾 sales feed.

Tile ownership is race-safe via `INSERT ... ON CONFLICT (x,y) DO NOTHING RETURNING` (claim first, charge after — a failed charge rolls back the claim in the same txn). The frontend renders a **DOM grid board** (`renderFarmBoard`/`scaleFarmPots`/`loadFarm`) with a clicked-cell inspector (`renderFarmSelection`) and RPC wrappers (`buyFarmTile`/`levelUpCard`/`plantCrop`/`harvestCrop`/`sellCrop`). A **📋 Widok tabeli** toggle (`toggleFarmView`/`renderFarmTable`, `FARM_TABLE_COLS`) mirrors the same tile data as a sortable/filterable table (per-column header menus, live growth patching via `updateFarmTableGrowth`). `tagZenBlocks()` tags each migration tile as a plant cell. The **loot box UI lives in the Sklep tab** (`#farm-lootbox-section`, `loadFarmLootbox`/`renderFarmLootboxStore`/`openFarmLootbox`), not the farm. Realtime (`setupFarmRealtime`/`applyFarmTileChange`) patches tiles and live market prices; a 1s `startFarmTimer` advances growth bars (`updateFarmGrowth`). The 🎒 Mój Majątek (primary, gold) / 📖 Katalog / 📈 Cennik / ❓ Jak to działa / 📦 Skrzynki buttons sit in a horizontal **wooden top bar** (`#farm-topbar`, populated by `renderFarmPanels` via `farmToolbar()`) above a full-width field; the wooden `#farm-selection` tile inspector renders below the field (`renderFarmSelection` → `renderFarmSelectionBody`); the old `#farm-collection`/`#farm-market` side panels were removed, so **card leveling and crop selling live only in Mój Majątek**. The empty-tile plant picker renders full `farmCardTile` cards in pick mode (`fcc-grid`), not tiny emoji. Selling goes through a price-impact confirm modal (`farmSellConfirm`/`farmSellQuote`, a client replica of `sell_crop_to_npc`). The hub modal is plain white (the wood theme is kept only on the `.farm-section` side board). These buttons open a **single tabbed hub modal** `openFarmHub(initialTab)` (built on `openFarmModalShell`; calls `ensureFarmData()` first, so it works before the tab is opened) whose three tabs are swapped by `renderFarmHubBody()`/`refreshFarmHub()`: **📖 Katalog** (`buildFarmCatalogBody` → `farmCatalogTable`, shared `farmCatalogModel`/`farmCardFacts`/`farmCatalogGroups` — one rarity-grouped comparison table with per-card lootbox odds + grow/yield/NPC-price + ≈profit/day + owned/locked/NFT-supply markers, `farmGrowLabel()` formats minutes as days), **🎒 Mój Majątek** (`buildFarmInventoryBody` — NFT grid + cards with inline level-up + crops with ½/all sell controls and a price-trend chip, via shared `farmCropRow`/`farmTrendChip`, reusing `levelUpCard`/`sellCrop`; also hosts the land section `buildFarmLandSection` — a `farmTileDeed` ownership-deed card per owned tile + the `farmTaxStatusCard` land-tax panel + `farmTerritoryPriceTable`, all moved out of 📈 Cennik which now shows only crop prices. The hub tab id stays `inventory`; only its label changed from „Ekwipunek" to „Mój Majątek"), and **❓ Jak to działa** (`buildFarmHelpBody`/`renderFarmHelpInto`/`farmHelpSection` — in-game docs for every mechanic). The `farmToolbar` buttons open the same hub on the matching tab (the Portfel wallet quick link was removed). **`FARM_W`(=13)/`FARM_H`(=4) mirror `supabase/farm.sql`; the coin reasons are wired into `economy_stats()` (new `farm_assets` bucket + mint/burn reason lists) and `user_assets_value`/`user_net_worth_breakdown` — keep all three in sync (all three also subtract unpaid `land_tax_debt` from net worth — see the land-tax bullet above).** **Phase 2 — serialized NFT cards is now LIVE.** `farm_card_defs` rows with a non-NULL `edition_size` are legendary NFTs (`diamond_rose`/`golden_sunflower`/`crystal_lotus`/`aeae_banana` — the last being the apex card: the variegated Hawaiian „Ae Ae" banana, smallest edition (8), highest yield/price, so rarest+most expensive+most profitable); they drop from the **normal** `open_farm_lootbox` (low `draw_weight`) until the edition sells out — **supply and serials come from the monotonic `farm_card_defs.minted_count`, not live-instance counts** (merges burn instances; see `nft-merge-fixes.sql`) — and each draw mints a unique serial into `farm_nft_instances` (public SELECT, `UNIQUE(species,serial_no)`, minted under the def row lock). **Level lives on each instance** (`farm_nft_instances.level`); NFTs level by **merging two same-species same-level instances** (`level_up_nft(hero, fuel)`: hero +1 level, fuel burned forever, cost `50×level²`, both must be unlisted + unplanted). Each instance also gets a **unique funny person-name** drawn from the species' persona pool (`nft_name`, assigned at mint by per-pool mint order via `farm_nft_persona(species, idx)` → `farm_nft_pool(species)`: the `aeae_banana` draws **Hawaiian** names, while gendered Slavic species route through `farm_nft_name(idx, is_female)` + `farm_nft_is_female(species)` — feminine species like `diamond_rose` get women's names; backfilled for older rows); clicking an NFT in Mój Majątek opens a trading-card modal (`openFarmNftModal`). NFT instances and fungible plant cards both render with the **same fixed-size `.fcc` frame** (`farmCardTile`/`farmNftTile`), regardless of level. The lootbox `cards[]` carries `{nft:true, serial_no, edition_size, nft_name}`; the frontend (`fmNft` state) shows names+serials in the pack reveal, Mój Majątek **💎 Karty NFT** grid, and the catalog. Net worth values held plant cards by rarity (common 20 / rare 50 / epic 150, NFT species excluded) and each NFT instance at `round(20000/edition_size * level)` — wired into both `user_assets_value`/`user_net_worth_breakdown` (`leaderboard-net-worth-items.sql`) and `economy_stats()` (`economy-stats.sql`).

**Phase 3 — P2P card/NFT/territory resale is now LIVE** via `supabase/farm-marketplace.sql` (run after `marketplace.sql` + `farm.sql`). It extends `marketplace_listings` with item linkage (`item_kind` `farm_nft`/`farm_card`/`farm_tile`, `nft_instance_id`, `card_species`, `farm_tile_x/y`), reusing the Targowisko bid/escrow/refund engine for coins. `create_farm_nft_listing` (reserves the instance via `farm_nft_instances.listed`), `create_farm_card_listing` (reserves one duplicate by decrementing `farm_collection.count`), and `create_farm_tile_listing` (reserves an owned empty non-migration tile via `farm_tiles.listed`) make listings; `buy_marketplace_listing`/`settle_marketplace_listing` are item-aware (call `farm_marketplace_deliver` → transfer NFT `owner_id`, credit a level-1 fungible card, or transfer `farm_tiles.owner_id` and set `asset_value` to the sale price), and `cancel_marketplace_listing` calls `farm_marketplace_release`. Listed tiles cannot be planted (`plant_crop` rejects `tile_listed`) and render a small `FOR SALE` sign on the farm board. `farm_nft_transfers` (public SELECT) logs every NFT `mint`+`sale` with price; `open_farm_lootbox` writes a `mint` row (guarded). The `marketplace_cards` view exposes the item columns. Frontend: the create-listing modal (`#ml-overlay`) has a "Co sprzedajesz?" toggle (`mlSellKind`) + item picker (`mlPopulateItemPicker`) → `createFarmListing`; `buildMarketplaceCard` shows a Farma item badge; clicking an NFT row in the Katalog opens `openFarmNftExplorer` (every serial slot + owners + price history).
